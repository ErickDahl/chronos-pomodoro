import styles from './styles.module.css';

type {{pascalCase name}}Props = {
  title?: string;
};

const {{pascalCase name}} = ({ title }: Readonly<{{pascalCase name}}Props>) => {
  return (
    <div className={styles['{{camelCase name}}']}>
      <h1>{title}</h1>
    </div>
  );
};

export { {{pascalCase name}} };
